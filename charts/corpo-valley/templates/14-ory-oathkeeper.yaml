{{- if include "cv.role.platform" . }}
{{- $ns := include "cv.ns" (list $ "ory") }}
{{- $portalHost := include "cv.host" (list $ "portal") }}
{{- $giteaHost := include "cv.host" (list $ "gitea") }}
{{- $giteaSvc := include "cv.svc" (list $ "gitea" "gitea") }}
{{- $ketoRead := include "cv.svc" (list $ "ory-keto-read" "ory") }}
{{- $kratosPublic := include "cv.svc" (list $ "ory-kratos-public" "ory") }}
{{- $hydraAdmin := include "cv.svc" (list $ "ory-hydra-admin" "ory") }}
{{- $projectsWildcard := include "cv.projectsWildcard" $ }}
{{- $projectsHostSuffix := include "cv.projectsHostSuffix" $ }}
{{- $cooldepsPublic := and $.Values.cooldeps.enabled $.Values.cooldeps.publicIngress }}
{{- $cooldepsHost := include "cv.host" (list $ "cooldeps") }}
{{- $cooldepsSvc := include "cv.svc" (list $ "cooldeps" "cooldeps") }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ory-oathkeeper-config
  namespace: {{ $ns }}
  labels:
    {{- include "cv.labels" $ | nindent 4 }}
    app: ory-oathkeeper
data:
  oathkeeper.yaml: |
    serve:
      proxy:
        port: 4455
      api:
        port: 4456

    errors:
      fallback:
        - json
      handlers:
        json:
          enabled: true
          config:
            verbose: false
        redirect:
          enabled: true
          config:
            to: https://{{ $portalHost }}/login
            return_to_query_param: return_to
            when:
              - error: [unauthorized, forbidden]
                request:
                  header:
                    accept: [text/html]

    access_rules:
      matching_strategy: regexp
      repositories:
        - file:///etc/oathkeeper/config/rules-gitea.json
        - file:///etc/oathkeeper/config/rules-projects.json
        {{- if $cooldepsPublic }}
        - file:///etc/oathkeeper/config/rules-cooldeps.json
        {{- end }}

    authenticators:
      noop:
        enabled: true
      oauth2_introspection:
        enabled: true
        config:
          introspection_url: http://{{ $hydraAdmin }}:4445/admin/oauth2/introspect
          token_from:
            header: Authorization
      cookie_session:
        enabled: true
        config:
          check_session_url: http://{{ $kratosPublic }}:4433/sessions/whoami
          preserve_path: true
          extra_from: "@this"
          subject_from: "identity.id"
          only:
            - ory_kratos_session
      anonymous:
        enabled: true
        config:
          subject: anonymous

    authorizers:
      allow:
        enabled: true
      deny:
        enabled: true
      remote_json:
        enabled: true
        config:
          remote: http://{{ $ketoRead }}:4466/relation-tuples/check
          payload: |
            {
              "namespace": "services",
              "object": "{{ "{{ printIndex .MatchContext.RegexpCaptureGroups 0 }}" }}",
              "relation": "access",
              "subject_id": "{{ "{{ print .Subject }}" }}"
            }

    mutators:
      noop:
        enabled: true
      header:
        enabled: true
        config:
          headers:
            X-User-Id: "{{ "{{ print .Subject }}" }}"
            X-User-Email: "{{ "{{ print .Extra.identity.traits.email }}" }}"

  rules-gitea.json: |
    [
      {
        "id": "gitea-git-api-rule",
        "version": "v0.40.0",
        "match": { "url": "<https?://>{{ $giteaHost }}/<(api/.*|v2/.*|.+\\.git/.*)>", "methods": ["GET","POST","PUT","DELETE","PATCH","HEAD","OPTIONS"] },
        "authenticators": [{ "handler": "noop" }],
        "authorizer": { "handler": "allow" },
        "mutators": [{ "handler": "noop" }],
        "upstream": { "url": "http://{{ $giteaSvc }}:80", "preserve_host": true }
      },
      {
        "id": "gitea-ui-rule",
        "_comment": "Gitea web UI — gated to any signed-in Corpo Valley member by the Kratos session (cookie_session); Gitea itself enforces per-repo visibility (private repos are only visible to their collaborators). The edge authorizer is `allow`: it is NOT a Keto service check, because nothing grants `services:gitea#access` (gitea is a protected client the admin UI can't toggle, and bootstrap only seeds groups:ADMIN), so a Keto check here forbids every member — including admins — and the redirect error handler turns that into an infinite /login loop. Members reach the UI from the project-card 'Repo' links, so it must be member-accessible.",
        "version": "v0.40.0",
        "match": { "url": "<https?://>{{ $giteaHost }}/<(?!api/|v2/|.+\\.git/).*>", "methods": ["GET","POST","PUT","DELETE","PATCH"] },
        "authenticators": [{ "handler": "cookie_session" }],
        "authorizer": { "handler": "allow" },
        "mutators": [{
          "handler": "header",
          "config": { "headers": { "X-User-Id": "{{ "{{ print .Subject }}" }}", "X-User-Email": "{{ "{{ print .Extra.identity.traits.email }}" }}" } }
        }],
        "upstream": { "url": "http://{{ $giteaSvc }}:80", "preserve_host": true }
      }
    ]

  rules-projects.json: |
    [
      {
        "id": "projects-cookie-session",
        "version": "v0.40.0",
        "match": { "url": "<https?://><[a-z0-9-]+>.{{ $projectsHostSuffix | replace "." "\\." }}/<.*>", "methods": ["GET","POST","PUT","DELETE","PATCH","HEAD","OPTIONS"] },
        "authenticators": [{ "handler": "cookie_session" }],
        "authorizer": { "handler": "allow" },
        "mutators": [{ "handler": "noop" }],
        "upstream": { "url": "http://127.0.0.1" }
      }
    ]
{{- if $cooldepsPublic }}

  rules-cooldeps.json: |
    [
      {
        "id": "cooldeps-public-rule",
        "_comment": "Public cooldeps proxy (cooldeps.<domain>). Unauthenticated by design: the callers are package managers (npm/pip/go) and CI, which cannot complete a Kratos browser login, so the authenticator is noop and the authorizer allow. cooldeps is a read-only gating proxy in front of public registries — there is no tenant data behind it. Only rendered when cooldeps.publicIngress is on.",
        "version": "v0.40.0",
        "match": { "url": "<https?://>{{ $cooldepsHost }}/<.*>", "methods": ["GET","POST","PUT","DELETE","PATCH","HEAD","OPTIONS"] },
        "authenticators": [{ "handler": "noop" }],
        "authorizer": { "handler": "allow" },
        "mutators": [{ "handler": "noop" }],
        "upstream": { "url": "http://{{ $cooldepsSvc }}:8080", "preserve_host": true }
      }
    ]
{{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ory-oathkeeper
  namespace: {{ $ns }}
  labels:
    {{- include "cv.labels" $ | nindent 4 }}
    app: ory-oathkeeper
spec:
  replicas: {{ $.Values.scale.oathkeeper | default 1 }}
  selector:
    matchLabels:
      app: ory-oathkeeper
  template:
    metadata:
      labels:
        app: ory-oathkeeper
      annotations:
        # Bump on any rules/config change — the rules are a mounted ConfigMap and
        # oathkeeper doesn't hot-reload, so the pod must roll to pick them up.
        config-revision: "2026-06-12-gitea-ui-allow{{ if $cooldepsPublic }}-cooldeps{{ end }}"
    spec:
      imagePullSecrets:
        - name: {{ $.Values.image.pullSecret }}
      containers:
        - name: oathkeeper
          image: oryd/oathkeeper:v0.40
          args: [serve, --config, /etc/oathkeeper/config/oathkeeper.yaml]
          ports:
            - { name: proxy, containerPort: 4455 }
            - { name: api,   containerPort: 4456 }
          volumeMounts:
            - { name: config, mountPath: /etc/oathkeeper/config }
          resources:
            {{- toYaml $.Values.resources.oathkeeper | nindent 12 }}
          livenessProbe:
            httpGet: { path: /health/alive, port: 4456 }
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet: { path: /health/ready, port: 4456 }
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: ory-oathkeeper-config
---
apiVersion: v1
kind: Service
metadata:
  name: ory-oathkeeper-proxy
  namespace: {{ $ns }}
  labels:
    {{- include "cv.labels" $ | nindent 4 }}
    app: ory-oathkeeper
    component: proxy
spec:
  selector:
    app: ory-oathkeeper
  ports:
    - { protocol: TCP, port: 4455, targetPort: 4455 }
---
apiVersion: v1
kind: Service
metadata:
  name: ory-oathkeeper-api
  namespace: {{ $ns }}
  labels:
    {{- include "cv.labels" $ | nindent 4 }}
    app: ory-oathkeeper
    component: api
spec:
  selector:
    app: ory-oathkeeper
  ports:
    - { protocol: TCP, port: 4456, targetPort: 4456 }
{{- if $.Values.hosts.oathkeeperWildcard }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ory-oathkeeper
  namespace: {{ $ns }}
  labels:
    {{- include "cv.labels" $ | nindent 4 }}
    app: ory-oathkeeper
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffer-size: "128k"
    {{- with $.Values.ingress.commonAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ $.Values.ingress.className }}
  rules:
    - host: "*.{{ $.Values.domain }}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ory-oathkeeper-proxy
                port:
                  number: 4455
{{- end }}
{{- end }}
